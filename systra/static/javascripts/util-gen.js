// Generated by CoffeeScript 1.6.3
var adjustMenu, iniciarMenu, ww;

ww = document.body.clientWidth;

window.cargarProfile = function(fn) {
  return $.ajax({
    type: 'GET',
    url: '/getProfile',
    success: function(data) {
      var collection, item, _i, _len;
      collection = [];
      for (_i = 0, _len = data.length; _i < _len; _i++) {
        item = data[_i];
        collection.push(item);
      }
      window.profile = collection;
      fn();
    }
  });
};

iniciarMenu = function() {
  $(".nav li a").each(function() {
    if ($(this).next().length > 0) {
      return $(this).addClass("parent");
    }
  });
  $('.toggleMenu').on('click', function(e) {
    e.preventDefault();
    $(this).toggleClass('active');
    return $(".nav").toggle();
  });
  adjustMenu();
  return $(window).bind('resize orientationchange', function() {
    ww = document.body.clientWidth;
    return adjustMenu();
  });
};

adjustMenu = function() {
  if (ww < 768) {
    $(".toggleMenu").css("display", "inline-block");
    if (!$(".toggleMenu").hasClass("active")) {
      $(".nav").hide();
    } else {
      $(".nav").show();
    }
    $(".nav li").unbind('mouseenter mouseleave');
    return $(".nav li a.parent").unbind('click').bind('click', function(e) {
      e.preventDefault();
      return $(this).parent("li").toggleClass("hover");
    });
  } else if (ww >= 768) {
    $(".toggleMenu").css("display", "none");
    $(".nav").show();
    $(".nav li").removeClass("hover");
    $(".nav li a").unbind('click');
    return $(".nav li").unbind('mouseenter mouseleave').bind('mouseenter mouseleave', function() {
      return $(this).toggleClass('hover');
    });
  }
};

window.sel = function(select, valor) {
  var ctl, i, _i, _ref;
  console.log("Entramos buscar el elemento");
  ctl = document.getElementById("delegacion_miguel");
  alert(ctl.options.length);
  for (i = _i = 0, _ref = ctl.options.length; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
    alert(ctl.options[i]);
  }
};

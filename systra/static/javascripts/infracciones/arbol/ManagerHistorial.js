// Generated by CoffeeScript 1.6.3
define(['core/sandbox'], function(sandbox) {
  var ManagerHistorial;
  return ManagerHistorial = {
    iniciar: function() {
      var that;
      that = this;
      this.sandbox = new sandbox(this);
      this.sandbox.on('show-add-block', this.showAddBlock);
      this.sandbox.on('hidden-add-block', this.hiddenAddBlock);
      this.sandbox.on('show-history-block', this.showHistoryBlock);
      return this.sandbox.on('hidden-history-block', this.hiddenHistoryBlock);
    },
    showAddBlock: function() {
      var cambios, el;
      $("#agregar-bloque").html('<span class="icosystra-history"></span>');
      el = $("#folio-agregar-folio");
      cambios = {
        height: "auto",
        width: "100%",
        margin: "1em",
        padding: "1em",
        overflow: "hidden",
        transition: "all 2s ease"
      };
      return el.css(cambios);
    },
    showHistoryBlock: function() {
      var cambios, el;
      $("#agregar-bloque").html('<span class="icosystra-plus"></span>');
      el = $("#historia-bloques");
      cambios = {
        height: "auto",
        width: "100%",
        margin: "1em",
        padding: "1em",
        overflow: "hidden",
        transition: "all 2s ease"
      };
      return el.css(cambios);
    },
    hiddenAddBlock: function() {
      var cambios, el;
      $("#agregar-bloque").html('<span class="icosystra-plus"></span>');
      el = $("#folio-agregar-folio");
      cambios = {
        height: "0",
        width: "0",
        margin: "0",
        padding: "0",
        overflow: "hidden",
        transition: "all 2s ease"
      };
      return el.css(cambios);
    },
    hiddenHistoryBlock: function() {
      var cambios, el;
      $("#agregar-bloque").html('<span class="icosystra-history"></span>');
      el = $("#historia-bloques");
      cambios = {
        height: "0",
        width: "0",
        margin: "0",
        padding: "0",
        overflow: "hidden",
        transition: "all 2s ease"
      };
      return el.css(cambios);
    }
  };
});
